// cancel animation
? item.left.state = 3
  equipL wand *0 +0
  equipL @item.left@
? item.right.state = 3
  equipR shield *0 +0
  equipR @item.right@

var pickpocketbuff = ＂pick_pocket:＂ + 3

? loc = mine
  ? loc.begin &
  ^ item.potion = empty
    brew wood + bronze // berserk potion 

  ? foe.distance > 17
    equipL triskelion
    equipR compound shield *10
  ? foe.distance <= 17
    equipL triskelion
    equipR dashing shield *10
  ? foe.distance <= 11
    equipL sword *0 +0
    equipR compound shield *10
  ? foe.distance <= 6
    equip skeleton arm
    
  ? item.CanActivate()
    /* INVOKE CINDERWISP */   
    ? summon.GetId() ! ＂cinderwisp＂
      equipR fire talisman
      activate R

    /* USE PICK POCKET */
    :? item.GetCooldown(＂skeleton_arm＂) <= 0 &
    ^  buffs.string = pickpocketbuff &
    ^  foe.distance <= 6
       equip skeleton arm
       activate R

    /* USE BLADE OF THE FALLEN GOD */
    :? item.GetCooldown(＂blade＂) <= 0 &
    ^  foe.count >= 5
       equip blade
       activate R

    /* USE QUARTERSTAFF DASH */   
    :? item.GetCooldown(＂quarterstaff＂) <= 0
      equip quarterstaff *10
      activate R

  : /* CAN'T ACTIVATE ITEMS */
    equipL triskelion
    equipR compound shield *10
    ? hp < maxhp
      equipR ouroboros

  /* BOSS */ 
  ? foe = bronze_guardian &
  ^ item.CanActivate()
    equipL aether wand dU *10
    equipR aether wand D *10

    /* APPLY DEBUFFS */
    ? buffs.string = smite &
    ^ foe.debuffs.string ! debuff_damage &
    ^ foe.debuffs.string ! debuff_dot
      equipL poison wand dP *10
      equipR fire wand dF *10
      
    ? item.GetCooldown(＂mask＂) <= 0
      equipR mask
      activate R

    ? item.potion = berserk
      activate potion

    /* DODGE BRONZE GUARDIAN HAMMER HIT */
    ? foe.state = 32 & foe.time >= 30
      equipL mind
      equipR mask

? pickup.distance <= 10
  equipL triskelion
  equipR star

>@totaltime@/@loc.averageTime@ (@loc.bestTime@)
