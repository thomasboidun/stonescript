func formatTime(frames)
  var s = frames / 30
  var m = s / 60
  var h = m / 60

  s = s % 60
  m = m % 60

  ? h < 0
    h = 0
  ? m < 0
    m = 0
  ? s < 0
    s = 0

  ? h < 10
    h = "0" + h
  ? m < 10
    m = "0" + m
  ? s < 10
    s = "0" + s

  return h+":"+m+":"+s
